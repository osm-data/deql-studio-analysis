[project]
name = "deql-studio-analysis"
version = "0.1.0"
description = "Solver benchmarks for DeQL Studio (VLDB 2026 Demo Track)"
requires-python = ">=3.11"
dependencies = [
    "numpy==2.4.2",
    "highspy==1.13.1",
    "ortools==9.15.6755",
]

[project.optional-dependencies]
gurobi = ["gurobipy>=11.0"]
